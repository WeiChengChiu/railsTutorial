<h2><%= @event.name %></h2>
<h5><%= @event.status %></h3>

<% @event.groups.each do |g| %>
  <li><%= g.name %></li>
<% end %>

<%= simple_format @event.description %>

<%= link_to 'Back to index', events_path %>